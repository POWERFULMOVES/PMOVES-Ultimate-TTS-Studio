# Minimal runtime deps for the Gradio UI and VibeVoice tab.
# Optional engines (Fish Speech, F5, etc.) are disabled automatically when deps are missing.
gradio==6.7.0
numpy==1.26.0
scipy
soundfile
librosa==0.10.0
huggingface-hub
transformers==4.51.3
safetensors
tqdm
pydub
imageio-ffmpeg
requests
sentencepiece


# Security fixes for HIGH CVEs
pillow>=12.1.1
setuptools>=78.1.1
urllib3>=2.6.3
wheel>=0.46.2
brotli>=1.2.0
